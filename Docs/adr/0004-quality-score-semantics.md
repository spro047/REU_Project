# Quality Score Semantics

The video overlay will display a predicted Suture quality score, component scores, and Model confidence as separate values. Evaluation reports may additionally display the expert reference score and prediction error; the 1-10 prediction will not be called accuracy.
