# Pilot Scope and Grouped Splitting

The initial nine-video collection is a pilot feasibility dataset. Train, validation, and test data will be split by video and by doctor/operator when metadata permits, preventing near-duplicate frames from the same recording from leaking across evaluation sets.
