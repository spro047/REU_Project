# Hybrid CVAT Annotation

The project will use local/self-hosted CVAT with manual expert seed annotations followed by AI-assisted pre-annotation, expert correction, and iterative retraining. Manual expert labels remain the ground truth because automated labels cannot validate themselves.
